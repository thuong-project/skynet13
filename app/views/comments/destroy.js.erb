$("[data-post-id='<%= @post.id %>']").find("ul[name='comments']").find(
  "li[data-url='<%= comment_path(@post, @comment) %>']"
).remove();
$("[data-post-id=\"<%= @post.id %>\"] [name=\"count-comments\"]").
html('<%= @post.comments.size %>')