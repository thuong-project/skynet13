
$("[data-post-id=\"<%= @post.id %>\"] ul[name=\"comments\"]").
prepend("<%= escape_javascript( render'comments/comment', comment: @comment, post: @post ) %>");

$("[data-post-id=\"<%=@post.id%>\"] textarea").val("");
$("[data-post-id=\"<%= @post.id %>\"] [name=\"count-comments\"]").
html('<%= @post.comments.size %>')